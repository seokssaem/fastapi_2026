# ⚾ KBO 타자 기록 기반 OPS 예측 모델

## 1. 프로젝트 소개

### 프로젝트명

**타자의 경기 기록을 이용한 OPS 예측 모델 개발**

### 프로젝트 목적

KBO 타자의 기본 경기 기록을 활용하여 타자의 공격력을 나타내는 대표적인 지표인 `OPS(On-base Plus Slugging)`를 예측하는 머신러닝 회귀 모델을 개발한다.

데이터 전처리부터 모델 학습 및 평가까지의 과정을 하나의 분석 파이프라인으로 구성하고, 분석 코드를 함수 및 모듈 단위로 분리하여 유지보수와 재실행이 쉽도록 구현하는 것을 목표로 한다.

---

## 2. 분석 문제 정의

OPS는 타자의 공격력을 종합적으로 평가하는 대표적인 지표이다.

본 프로젝트에서는 타자의 타석, 타수, 안타, 홈런, 볼넷 등의 기본 경기 기록을 독립변수로 사용하여 OPS를 예측하는 머신러닝 모델을 구축한다.

### 분석 가설

* **H0(귀무가설)**: 타자의 기본 기록은 OPS 예측에 유의한 영향을 미치지 않는다.
* **H1(대립가설)**: 타자의 기본 기록은 OPS 예측에 유의한 영향을 미친다.

---

## 3. 데이터 및 변수

### 데이터

* 데이터 파일: `hitter.csv`
* 대상: KBO 타자 경기 기록
* 최소 타석 기준: `100타석 이상 선수`

표본의 신뢰도를 높이기 위해 타석이 100 이상인 선수만 분석 대상으로 선정하였다.

### 독립변수(X)

| 변수  | 설명  |
| --- | --- |
| PA  | 타석  |
| AB  | 타수  |
| H   | 안타  |
| 2B  | 2루타 |
| 3B  | 3루타 |
| HR  | 홈런  |
| BB  | 볼넷  |
| SO  | 삼진  |
| SB  | 도루  |
| RBI | 타점  |
| R   | 득점  |

### 종속변수(Y)

**OPS**

OPS는 출루율(OBP)과 장타율(SLG)의 합으로 계산되는 지표이다.

따라서 OBP와 SLG를 독립변수에 포함하면 종속변수인 OPS를 직접적으로 재구성할 수 있어 **데이터 누출(Data Leakage)** 문제가 발생할 수 있으므로 독립변수에서 제외하였다.

---

## 4. 개발 환경 및 사용 기술

### Programming Language

* Python

### 데이터 분석

* pandas
* NumPy
* SciPy

### 데이터 시각화

* Matplotlib
* Seaborn

### 머신러닝

* scikit-learn

### 주요 분석 도구

* `train_test_split`
* `KFold`
* `cross_val_score`
* `LinearRegression`
* `RandomForestRegressor`
* `mean_absolute_error`
* `mean_squared_error`
* `r2_score`

---

## 5. 프로젝트 구조
전처리 → 모델 학습 → 평가 과정을 하나의 파이프라인으로 구조화하고, 각 단계를 함수/모듈 단위로 분리해
유지보수와 재실행이 쉽도록 구성하였다. scikit-learn을 기반으로 구현하였다.

```text
kbo_ops/
├── hitter.csv
├── README.md
├── main.py               # 실행하는 py
└── ops_pipeline/         # 분석 로직 모듈
    ├── config.py         # 공통 설정값 (경로, 변수 목록, 하이퍼파라미터)
    ├── preprocessing.py  # 1단계: 데이터 로드 & 전처리
    ├── models.py         # 2단계: 데이터 분할 & 모델 학습 (선형회귀 / RandomForest)
    ├── evaluate.py         # 3단계: 성능 평가 (MAE, RMSE, R2, 5-Fold 교차검증)
    ├── visualize.py        # 시각화 함수 (분포, 상관관계, 예측 비교, 변수 중요도)
    ├── __init__.py         # 패키지 초기화 
    └── pipeline.py         # 전처리→학습→평가를 하나로 묶은 파이프라인
```

### 모듈별 역할

| 파일명 | 역할 | 상세설명 |
| ----- | ----- | ----- |
| `hitter.csv` | **원본 데이터** | KBO 타자 기록 데이터로, OPS 예측 모델의 학습 및 분석에 사용 |
| `config.py` | **환경 및 변수 설정** | 데이터 경로, 최소 타석 기준, Train/Test 비율, 교차검증 횟수, 독립변수와 종속변수 등 프로젝트에서 공통으로 사용하는 설정값 관리|
| `preprocessing.py` | **데이터 전처리** | CSV 데이터 불러오기, 최소 타석 기준 선수 필터링, 분석에 필요한 독립변수 및 종속변수 선택|
| `models.py`  | **데이터 분할 및 모델 학습** | Train/Test 데이터 분할, K-Fold 교차검증 설정, Linear Regression 및 Random Forest Regressor 모델 생성·학습 |
| `evaluate.py` | **모델 성능 평가** | MAE, RMSE, R²를 이용한 Test 데이터 평가와 K-Fold 교차검증 수행 및 모델별 성능 비교 |
| `visualize.py` | **데이터 및 모델 결과 시각화** | Boxplot, Histogram, 상관관계 Heatmap, 실제값-예측값 비교, 변수 중요도 그래프 생성 |
| `pipeline.py` | **전체 분석 파이프라인 실행** | 데이터 전처리 → Train/Test 분할 → 모델 학습 → 모델 평가 과정을 하나의 파이프라인으로 통합하여 실행|
| `__init__.py`| **패키지 초기화** | `ops_pipeline` 폴더를 Python 패키지로 구성하고 각 분석 모듈을 불러올 수 있도록 설정|
| `main.py`| **전체 분석 실행 및 결과 저장** | 프로젝트의 메인 실행 파일로, 분석 파이프라인을 호출하여 전처리 → 데이터 분할 → 모델 학습 → 평가 과정을 실행한다. 모델 성능 비교 결과를 CSV 파일로 저장하고, 이상치·분포·상관관계·실제값과 예측값 비교·변수 중요도 등의 시각화 결과를 results/ 폴더에 저장한다.|
| `README.md`| **프로젝트 설명 문서** | 프로젝트 목적, 분석 과정, 파일 구조, 실행 방법, 모델 평가 결과 및 팀원별 역할 등을 문서화  |
| `results/` | **분석 결과 저장 폴더** | 모델 평가 결과 및 시각화한 `.png` 파일 등을 저장 |


---

## 6. 분석 파이프라인

전체 분석 과정은 다음과 같이 구성하였다.

```text
hitter.csv
     ↓
데이터 로드
     ↓
데이터 전처리
     ↓
EDA
     ↓
Train / Test 데이터 분할
     ↓
모델 학습
     ├── Linear Regression
     └── Random Forest Regressor
     ↓
5-Fold Cross Validation
     ↓
모델 성능 평가
     ├── MAE
     ├── RMSE
     └── R²
     ↓
모델 성능 비교
     ↓
변수 영향력 분석
     ↓
최종 결과 및 결론
```

분석 과정을 각각의 함수와 모듈로 분리하고 `pipeline.py`에서 통합하여 동일한 분석을 쉽게 재실행할 수 있도록 구성하였다.

---

## 7. 데이터 전처리

### 7.1 최소 타석 필터링

선수별 기록의 표본 신뢰도를 확보하기 위해 **100타석 이상인 선수만 분석 대상**으로 선정하였다.

### 7.2 변수 선택

모델 학습에는 다음 11개의 변수를 사용하였다.

```python
FEATURES = [
    "타석", "타수", "안타", "2루타", "3루타",
    "홈런", "볼넷", "삼진", "도루", "타점", "득점"
]

TARGET = "OPS"
```

### 7.3 데이터 품질 확인

모델 학습 전 다음 항목을 확인하였다.

* 데이터 타입
* 결측치
* 이상치
* 변수별 데이터 분포
* 변수 간 상관관계

---

## 8. 탐색적 데이터 분석(EDA)

### 이상치 분석

Boxplot을 이용하여 각 독립변수와 OPS의 이상치 분포를 확인하였다.

### 변수 분포 분석

Histogram을 이용하여 각 변수의 데이터 분포를 확인하고 왜도(Skewness)를 계산하였다.

### 상관관계 분석

상관계수와 Heatmap을 이용하여 독립변수 간 관계와 각 독립변수와 OPS 사이의 관계를 분석하였다.

---

## 9. 데이터 분할

전체 데이터를 다음과 같이 학습용 데이터와 테스트용 데이터로 분리하였다.

* Train Data: **80%**
* Test Data: **20%**
* `random_state = 42`

또한 모델의 일반화 성능을 확인하기 위해 **5-Fold Cross Validation**을 수행하였다.

---

## 10. 모델 개발
OPS는 연속형 수치 데이터이므로 지도학습 중 **회귀(Regression)** 모델을 사용하였다.

| 모델 | 선정 이유 |
|---|---|
| Linear Regression | 변수 간 선형 관계 확인 가능, 결과 해석이 쉬움, Baseline 모델 |
| Random Forest Regressor | 비선형 관계 학습 가능, 변수 간 복잡한 관계 반영, 앙상블로 과적합 완화 |


---

## 11. 모델 평가

모델의 성능은 다음 세 가지 지표를 이용하여 평가하였다.

### MAE (Mean Absolute Error)

실제값과 예측값 차이의 절댓값 평균으로, **값이 작을수록 좋은 모델**을 의미한다.

### RMSE (Root Mean Squared Error)

예측 오차를 제곱하여 평균을 계산한 후 다시 제곱근을 적용한 값으로, **값이 작을수록 좋은 모델**을 의미한다.

### R² (결정계수)

모델이 종속변수의 변동을 얼마나 설명하는지를 나타내며 일반적으로 **1에 가까울수록 설명력이 높은 모델**을 의미한다.

추가적으로 **5-Fold Cross Validation**을 이용하여 특정 Train/Test 데이터 분할에만 의존하지 않고 모델의 일반화 성능을 평가하였다.

### 성능 평가 결과
| 모델 | Test MAE  | Test RMSE | Test R²| CV R² | CV R² | CV MAE 평균 |
|---|---|---|---|---|---|---|
| Linear Regression | 0.0375 | 0.0541 | `0.7425` | 0.7789 | 0.0357 | 0.0352|
| Random Forest Regressor | 0.0528 | 0.0628 | 0.6533 |0.6451 |0.0641 | 0.0496 |
---

## 12. 분석 결과

두 회귀 모델의 성능을 비교한 결과, 본 데이터에서는 `Linear Regression`이 `Random Forest Regressor`보다 높은 `R²`를 기록하였다.

Random Forest는 비선형적인 관계를 학습할 수 있는 모델이지만, 본 데이터에서는 타자의 기본 기록과 OPS 사이의 관계가 비교적 선형적인 특성을 보여 Linear Regression이 더 안정적인 성능을 나타낸 것으로 판단하였다.

---

## 13. 변수 영향력 분석

### Linear Regression

선형회귀의 회귀계수를 분석한 결과 주요 변수는 다음과 같이 나타났다.

1. 홈런
2. 3루타
3. 안타

### Random Forest Regressor

Random Forest의 Feature Importance를 분석한 결과 주요 변수는 다음과 같이 나타났다.

1. 타점
2. 홈런
3. 2루타

두 모델 모두 **홈런, 2루타, 3루타 등 장타력과 관련된 기록이 OPS 예측에 중요한 역할을 하는 것으로 나타났다.**

---

## 14. 결론

본 프로젝트에서는 KBO 타자의 기본 경기 기록을 이용하여 OPS를 예측하는 머신러닝 회귀 모델을 개발하였다.

Linear Regression과 Random Forest Regressor를 학습하고 MAE, RMSE, R² 및 5-Fold Cross Validation을 통해 모델의 성능을 평가하였다.

분석 결과 타자의 기본 경기 기록만으로도 OPS를 일정 수준 이상 설명할 수 있었으며, 특히 홈런을 비롯한 장타 관련 지표가 OPS 예측에 중요한 역할을 하는 것으로 나타났다.

따라서 본 분석에서는 타자의 기본 기록이 OPS 예측에 영향을 미친다고 판단하였다.

> **가설 검정 결과**
>
> H0(귀무가설) 기각
> H1(대립가설) 채택

---

## 15. 팀원 및 역할 분담

| 팀원        | 담당 역할   |
| --------- | --------------- |
| 김동욱   | Streamlit으로  프론트엔드 (Frontend) 작업 |
| 김수완   | 전반적인 백엔드 (Backend) 작업 및 ppt 작성 |
| 홍은경   | 팀원 역할 분담 및 README.md 작성  |
| 공동 수행 | 모듈 분리 방법 및 프론트앤드 내용에 관한 전반적인 의견 조율, 서로의 결과물에 대한 피드백 |

프로젝트의 주요 의사결정 과정은 팀원 전체가 함께 논의하였으며, 이후 세부 분석 및 결과물 제작 업무를 역할별로 분담하였다.

---

## 16. 프로젝트 구현 특징

본 프로젝트는 단순히 하나의 Python 파일에서 분석을 수행하는 방식이 아니라 분석 과정별 기능을 함수와 모듈로 분리하였다.

이를 통해 다음과 같은 장점을 갖도록 구현하였다.

* 분석 코드의 재사용성 향상
* 코드 유지보수 용이
* 분석 과정별 기능 분리
* 오류 발생 위치 확인 용이
* 데이터 또는 모델 변경 시 재실행 용이
* 전처리 → 모델 학습 → 평가 과정의 일관된 실행 가능

이를 통해 데이터 분석 과정 전체를 하나의 **재사용 가능한 분석 파이프라인**으로 구조화하였다.

## 17. Streamlit 구현
- https://kbo-hitter.streamlit.app/